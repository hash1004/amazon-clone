# Reference screenshots

Drop the provided PNGs here so they travel with the repo:

- `home-01.png` … `home-04.png` — amazon.com logged-out home page
- `search-01.png` — amazon.com/s?k=gaming results page
- `pdp-01.png`, `pdp-02.png` — a real product detail page

The written spec in `../amazon-replication-spec.md` is self-contained if the
images are missing.
